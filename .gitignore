# Cache generat de proxy-ul YouTube (nu trebuie versionat)
api/cache/*.json

# Config local cu cheia API (daca folosesti varianta cu fisier separat)
api/config.local.php

# Imagini OG generate automat de stiri/og-image.php (se refac singure)
stiri/og-cache/
